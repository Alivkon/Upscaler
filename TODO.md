# Что нужно сделать

Список открытых задач. Одна строка — одна задача; у каждой написано, **зачем**,
потому что через месяц причина забывается раньше формулировки. Сделанное отсюда
удаляется: история остаётся в git и в `research/`.

Порядок внутри разделов — по убыванию важности, разделы между собой не
упорядочены.

## Витрина и источники

- [ ] **Взять под рабочий стол мастер покрупнее и перерезать 16:9 — 41 кадр из 282.**
      Кадр 16:9 сейчас режется из того же мастера, что и телефонный, а вверх
      мастер никто не тянет. У 41 работы он мелкий, и кадр выходит меньше
      1920 × 1080 — самый мелкий 1146 × 645 (vl-0365), три штуки не берут даже
      720p. Страница при этом зовёт файл «desktop background» и размера рядом
      не пишет: обещание экрана картинке, которой на экран не хватает.
      **Пока спрятаны** — `DESKTOP_GATE` в `gallery.js` не отдаёт кадр меньше
      1920 × 1080, и вместе с ним кадр пропадает из карты сайта.
      Нужно: (1) под рабочий стол выбирать скан по тому же правилу, что и под
      телефон, — самый мелкий из тех, которых ещё хватает, только планка своя;
      (2) перерезать эти 41 и снять порог. Все 41 — от мастеров, которые и
      телефонную планку 2160 × 3840 не берут, так что скан нужен другой, а не
      другая нарезка того же. Замер: `node -e` по `images/manifest/`; правило
      размера — `SHORT_SIDE` и шапка `~/repos/wallpaper-gen/museum.mjs`.

      **vl-0139 не хватает одного пикселя.** 23.08 с неё срезали рамку —
      left 87, right 80, — и кадр 16:9 вышел 1919 × 1079, то есть по единице
      ниже ворот с обеих сторон. Обои для рабочего стола у неё пропали.
      Лечится либо этим пунктом, либо одним пикселем рамки: left 86 даёт
      плиту 1920 и кадр ровно 1920 × 1080. Решает Charlie — это его обмер.

- [ ] **Мультикропы: у восьми работ по два-три варианта кадра, взят первый.**
      23.08 позиции из `research/to-crop-positions.md` накатаны — все 67 стоят
      в `museum-works.json` полем `crop` и пересобраны, кадры сверены с плитой
      попиксельно; витрина просмотрена целиком, работ без строки в листе не
      осталось. У vl-0060, vl-0179, vl-0240, vl-0291, vl-0359, vl-0377, vl-0381
      и vl-0382 записано по два-три варианта; применён `v1`, остальные лежат
      в листе неприменёнными.
      Десктопный кадр с 23.08 ставится отдельно: `crop` принимает правило на
      кадр (`{ phone: {...}, wide: {...} }`), позиции — в
      `research/to-crop-desktop.md`, накатано 90. Своё правило есть и у
      vl-0068, vl-0087, vl-0374 по два варианта. Не отсмотрены одиннадцать —
      vl-0377…vl-0387.
      Нужно придумать, чем они станут: отдельными слугами, суффиксом `-v2`
      или чем-то ещё. Заодно: сдвиг ставит все три кадра — `phone`, `tall`,
      `wide`, — а меряли его по телефонному; на 16:9 он упирается в край и
      двигает меньше, но двигает.

- [ ] **ColBase: 36 829 японских работ под CC BY, но потолок 3000 px.**
      Четыре национальных музея Японии отдают всё свободно, коммерческое
      использование разрешено, условие одно — подпись и слова об изменении;
      документы репозитория под это уже поправлены. Мешает не лицензия, а
      размер: замер сорока работ дал ровно 3000 px по длинной стороне у всех
      до единой, то есть ни 4K (нужно 3840), ни телефонная планка 3120 —
      не хватает 120 px. **Но этих 120 px не видно:** на кусках 1:1 растяжка
      1,04× от эталона не отличается, порог видимости около 1,73×. То есть
      планка 3120 отсекает ColBase по счёту, а не по виду, и решать надо не
      «брать ли», а что делать с планкой. Прежде этого стоит померить выход
      воронки на японской живописи — свитки и каллиграфия светлые и осыпятся
      на воротах.
      Разбор — `research/2026-08-22-japanese-museums.md`,
      замер — `node scripts/research/colbase-size.mjs`.

- [ ] **Повторить поиск по картинке у Кливленда — сейчас маршрут отдаёт 500.**
      `POST /api/artworks` (Appendix E) отвечает `500` на любой корректный файл,
      включая собственную картинку музея; `422` без файла и живой `GET`
      говорят, что дело за их валидатором, а не за нами. Зачем ждать: это
      единственный доступный взгляд «что НА картине» — наши двенадцать мер
      видят только, КАК она освещена, и потому вытягивают портреты
      (20.08). Всё готово: `node scripts/research/cle-similar.mjs --probe`,
      и если ответ узнаёт сам себя — запуск без ключа, 34 вопроса.
      Разбор — `research/2026-08-22-cleveland-by-eye.md`.

- [ ] **Развеска: витрина открывается четырьмя букетами ван Хёйсума подряд.**
      22.08 витрина стала ровно набором Чарли — 76 работ, — но порядок в
      `catalogue/order.json` остался прежним, а он складывался под другой,
      вчетверо большую коллекцию. Раньше однофамильцы расходились чужими
      работами между ними; теперь чужие сняты, и одинаковое встало рядом.
      Первые четыре карточки — «Vase with Flowers», «Vase of Flowers»,
      «Flowers in an Urn», «Still Life with Flowers and Fruit», дальше два
      Хаммерсхёя. Из 76 работ 7 — ван Хёйсум, 5 — Бертен, 5 — Одюбон.
      Развеска — вкус, а не вывод, поэтому сама она не тронута.
      Разбор — `research/2026-08-22-the-gallery-is-the-ticks.md`.

- [ ] **Две живые работы не дотягивают до телефонного пола 1320×2868.**
      vl-0057 (1288×2790, вышла на витрину 22.08) и vl-0236 (1311×2841, стояла
      и раньше). Обе есть в списке на Topaz ниже; теперь их видно посетителю,
      а не только листу отбора.

- [ ] **Посмотреть глазами подписи к версиям на странице работы.** 22.08 у
      41 работы появился второй ряд файлов — «Other versions», та же картина
      в другой обработке. Внутренние имена (`ceil`, `snap`, `niobe`) посетителю
      не говорят ничего, поэтому в `pages.js` заведена карта `TREATMENT_NAMES`:
      As scanned · Colour balanced · Muted · Muted, stronger · Dimmed · Dimmed,
      soft highlights. Это первый заход, и меняется он в одном месте.
      Заодно решить, нужен ли версии целый лист: сейчас ряд предлагает
      телефонный кадр, потому что его же отдаёт кнопка Download, а плита версии
      лежит на диске и со страницы не названа.
      Разбор — `research/2026-08-22-what-charlie-liked.md`.

- [ ] **Подключить `research/presets.json` к тем пяти файлам, что до сих пор
      носят числа руками.** Значения переехали в репозиторий 21.08, и `edits.mjs`
      их читает, а `.collections.mjs`, `.funnel.mjs`, `.treatset.mjs`,
      `box.mjs` и `sheet.mjs` — нет. Пока копий шесть, расхождения продолжатся:
      в записи `busy 0…70`, в `.collections.mjs` и `.funnel.mjs` — `[0, 75]`,
      а ворота по году `1200…1960` со снимка не доехали никуда.
      Разбор — `research/2026-08-21-six-versions-of-each.md`.

- [ ] **Прокрутка вверх на `/phone` — открыть `/scrolltest.html` на телефоне.**
      21.08 чинили дважды: сначала три причины рывков (раскодированные кадры,
      номер слайда через `window.innerHeight`, `backdrop-filter` на галочках),
      потом две направленные (привязка прокрутки — её внёс сам же рециклер,
      подставляя картинки ВЫШЕ экрана; и высота слайда в `dvh`, которая ходит за
      адресной строкой). Ответ Charlie на первый заход: «вверх сломано, вниз
      нормально». На десктопе вверх работает безупречно — десять настоящих
      касательных бросков, каждый на один слайд, промах не больше 2 px, так что
      причину там не видно.
      `scrolltest.html` — тот же лист **без фотографий**, цифра на цвете, четыре
      переключателя (высота, режим снапа, привязка, фотографии) и журнал
      приземлений: пропуск виден как дыра — `40, 39, 36`. Это и надо прочитать
      с телефона, дальше гадать нечего.
      Разбор — `research/2026-08-21-phone-feed-scrolling.md`.

- [ ] **Спросить `provenance.page` за все 410 мелких работ, а не за 12.**
      В каталоге лежит 330 проверенных адресов источника, и пул их никогда не
      читал. Двенадцать нашлись только потому, что уже опубликованы; остальные
      ищутся так же — по картинке, а не по названию. Три маршрута готовы в
      `recover.mjs` (Викисклад, Кливленд, SMK).
      Разбор — `research/2026-08-21-the-catalogue-knew.md`.

- [ ] **Писать в манифест, из какого файла сделана пластина.**
      `images/manifest/*.json` помнит пластину и обработку, но не источник.
      Из-за этого одиннадцать работ пришлось качать заново по сети за тем, что
      когда-то лежало на этом же диске.

- [ ] **Одна картина Хаммерсхёя записана дважды: vl-0258 и vl-0260.**
      Две загрузки одного скана с Викисклада, 3726×3875 и 3729×3876, получили
      два слага и две записи. Сверено глазами — картина одна.
      22.08 обе ушли с витрины вместе с остальными неотмеченными, так что за
      один запрос они больше не соревнуются, и это **уже не срочно**. Но две
      записи на один холст в каталоге остались, и стоит одной из них вернуться
      на витрину — вернётся и конкуренция. Свести к одной.
      Разбор — `research/2026-08-21-one-copy-per-painting.md`.

- [ ] **vl-0065 отрисован в 3400 px, хотя источник даёт 6000.** Единственная
      работа в галерее, которая реально не дотягивает до нормы 3840: остальные
      24 «маленькие» уже на норме. «Rest on the Flight into Egypt», источник
      4483×6000 лежит на диске.

- [ ] **Искать лучший файл той же картины, а не большую версию того же файла.**
      Это та самая ошибка, из-за которой Хид «Sunlight and Shadow» лежал в
      наборе как 709×323, хотя на Викискладе есть 12541×5626 — под другим
      именем. Проверка `originals.mjs` спрашивала «этот файл больше?», а нужно
      «есть ли файл этой картины лучше?». Сделать поиск по 25 отмеченным
      работам, у которых вертикальный кроп уже 450 px. Разбор —
      `research/2026-08-21-the-same-painting-twice.md`.

- [ ] **Разобрать 50 групп дубликатов, которые никто не смотрел.** Восемь, где
      от выбора что-то зависит, проверены глазами и перечислены в записке.
      Остальные ничего не ломают — обе копии в наборе, — но раздувают 628 и
      тратят время глаз. Лист: `dupes.html`.

- [ ] **Найти другой файл для 13 отмеченных работ, которые меньше 1100 px.**
      Апскейл источника тут не поможет: на Викискладе лежит оригинал, и он
      правда такого размера. Единственный путь — другой файл той же картины,
      а это поиск, а не переписывание ссылки. Самые мелкие: Мартин «Christ
      Stilleth the Tempest» 600 px, «Landscape (1677)» 700 px, два Хаммерсхёя
      меньше 800. Список — в `research/2026-08-20-bigger-sources.md`.

- [x] ~~Починить обрезку рамки на косом снимке.~~ **Автоматический резак снят
      с довольствия 23.08.** Чинить нечего: детектор (`/tmp/oils-preview/frames.mjs`)
      снимал стену вокруг рамки надёжно, а саму рамку — редко, и на `pick.html`
      из 33 обрезанных 10 масляных работ остались в рамке. Ни одна плита, ни одна
      запись каталога по нему никогда не резалась: `frames.json` так и остался
      черновиком. Рамка теперь снимается только рукой — обмер в
      `research/crop-ruler.html`, поле `trim` в `museum-works.json`, порядок —
      `research/how-to-crop-frames.md`. Почему автомат не вышел —
      `research/2026-08-20-cutting-the-frame-off.md` и
      `research/2026-08-20-the-picking-sheet.md`.

- [ ] **Перенести правку hires.mjs про TIFF в сам скрипт.** `upgrade.mjs` уже
      забрал 14 работ, которые он пропустил, но сам `hires.mjs` всё ещё
      промахивается на `lossy-page1-500px-NAME.tif.jpg`, и следующий сбор
      наступит на те же грабли. Причина и замеры —
      `research/2026-08-20-bigger-sources.md`.

- [ ] **Обрезать кромку холста у всех работ отобранного набора, а не у одной.**
      Делать это по всей коллекции незачем: обрезка стоит времени и решается
      глазами, а показывать будем не всё. Отсюда порядок — сначала отбор,
      потом обрезка того, что осталось.
      Проверено на vl-0341 (Харнетт, Мюнхенский натюрморт): скан из Далласа
      несёт светлую кромку холста со всех четырёх сторон и полосу подрамника
      снизу. Коробка `{left: 96, top: 110, width: 7824, height: 6378}`
      из 8006×6587, то есть 4% площади; кадр 6587 → 6378, порога это не
      касается. Готовый файл и сравнение «до/после» — в записках смены.
      Способ, которым она найдена, работает и не требует `deframe`: средняя
      яркость по строкам и столбцам на полном разрешении, полоса 240 px
      с каждого края, граница — там, где яркость садится на полку картины.
      **Одна ловушка стоила прохода:** снизу между картиной и подрамником
      лежит тёмный зазор, и порог «полка + 8» принял его за картину,
      остановившись на 51 px вместо 98. Край надо смотреть до самого конца
      полосы, а не до первого падения; профиль печатать и читать глазами.

- [ ] **Поставить два потолка: цвет 18, яркость 65 — и яркие выбрасывать, а не
      гасить.** Числа выбраны Чарли на `/dark` 19.08, цена показана и принята:
      «надо платить дорогую цену». Конвейер пока не тронут. Цвет 18 почти ничего
      не стоит — 587 работ из 628 уже под ним. Яркость 65 стоит очень много: под
      ней всего 92 работы из 628, то есть выброс убирает 85% отбора после того,
      как цепочка уже сжала 1193 → 628. По вкусу число попадает: из семи работ
      Чарли на листе шесть проходят по яркости и все семь по цвету. «Два колибри
      и орхидея» — сознательное исключение: работа уже в
      `research/handpicked-overrules.json`, а `funnel.mjs` применяет ручные
      исключения после всех ворот, так что ради неё ничего расширять не нужно.
      Перед тем как делать, остаётся решить одно: «ярче 65» — это до затемнения
      или после? После — остаётся 92 работы, до — 38.
      Цифры и поверхность замера:
      [research/2026-08-19-the-two-ceilings-charlie-chose.md](research/2026-08-19-the-two-ceilings-charlie-chose.md).

- [ ] **Перенести плечо по светам в конвейер, если оно устроит на глаз.**
      На `/dark` оно уже есть и заменило ползунок теплоты: теплота гасила красный,
      а всё, что остаётся, если убрать из оранжевого красный, — зелёный (у де Витте
      средний тон уходит с 54° на 74° уже при охлаждении на 20). Плечо решает
      другую задачу — опускает света и не трогает тени, — и это тот самый ползунок
      «сделать как у Ниобы»: разброс тонов у неё 25 при медиане пула 94, а простым
      затемнением до такого разброса можно дойти только вдвое темнее, чем нужно.
      Кнопка «make them like Niobe» ставит весь отбор на 19/20/22 по разбросу.
      72 работы плечо не достаёт: у них тона ниже колена уже шире потолка —
      возможно, колено надо считать по работе, а не одно на всех.
      Замеры, проверка и что именно приблизительно, а что точно:
      [research/2026-08-19-the-shoulder-replaces-warmth.md](research/2026-08-19-the-shoulder-replaces-warmth.md).

- [ ] **Перенести баланс серого в конвейер вслед за плечом.**
      На `/dark` он уже стоит и принят Чарли 20.08 по картинкам («most got
      better»), мера живёт в репозитории — `scripts/research/grey-balance.mjs`.
      Работает так: берётся пятая часть самых бесцветных пикселей (те, что
      художник задумывал серыми) и уводится в нейтраль; коэффициенты нормируются
      так, что наибольший равен 1, поэтому операция умеет только убавлять свет.
      Сила — половинная: на полной 12 работ из 599 уходят в зелень и золото
      Лоррена синеет, на половинной — одна работа. 567 работ из 628 поправлены,
      61 не тронута вовсе, медиана потери света 2%.
      **Это не ползунок и им быть не может:** увод — брак копии, а не решение о
      том, как работа должна выглядеть, поэтому он впекается в кадр до того, как
      страница его увидит. Плюс живым ползунком он сломал бы точность разброса
      тонов: плечо монотонно по яркости, а баланс — нет, и квартили пришлось бы
      считать заново по исправленным пикселям, чего на странице сделать нельзя.
      Чего измерение не может в принципе: у картины, написанной в золотом свете,
      серые действительно золотые. Отличить пожелтевший лак от замысла по одному
      файлу нельзя, поэтому сила — вопрос вкуса, а не вывода.
      Замеры, обе кучи и проверка инварианта: [research/2026-08-20-grey-anchor-white-balance.md](research/2026-08-20-grey-anchor-white-balance.md).
      Сравнение «до/после» на 599 работах пересобирается: `node wbsheet.mjs`.

- [ ] **Решить по глазам, годится ли порядок «ближе к любимым» — он уже на `/dark`.**
      Поставлено 20.08: два флажка в существующем ряду, панель не выросла —
      «only the favourites box» (прячет 321 работу, остаётся 307) и «nearest a
      favourite first» (переупорядочивает сетку). Работы, названные Чарли,
      помечены ★ акцентным цветом: если его собственные выборы рассыпаны по
      порядку, значит порядок меряет не то. Сейчас шесть из первых двенадцати —
      его звёзды, между ними и лежат предложения.
      Меры — `iqr`, `p95`, `luma`, взяты по одной из семьи, а не подбором:
      `iqr`, `range` и `sd` — три имени одной величины, и взять все двенадцать
      значило бы утроить вес разброса тонов. Жадный поиск независимо выбрал те же
      три и остановился, но это подтверждение, а не причина.
      Замерено 20.08: коробка вокруг любимых — неверная форма, потому что работы
      Чарли лежат двумя кучками, а прямоугольник вокруг двух кучек содержит всю
      середину. Расстояние справляется. Проверка честная: спрятать одну любимую
      работу в пуле и ранжировать всё по близости к остальным — она всплывает на
      15-м процентиле при случайности 50. Меры, которые это несут: разброс тонов,
      затем цвет, затем яркость, и на трёх набор останавливается.
      Отдельно любопытно: `luma` в одиночку даёт 38% — почти случайность. Яркость
      делает почти всю ОТСЕЧКУ и почти бесполезна для ПОИСКА; ворота и поиск —
      разные задачи.
      Ловушка: расстояние видит свет, а не сюжет. Из 25 ближайших работ 17
      выброшены цепочкой, и добрая половина из них — портреты. Значит сортировать
      надо то, что цепочку уже прошло; чинить меру не нужно, для сюжета есть
      фильтр имён и модель.
      [research/2026-08-20-finding-more-like-the-favourites.md](research/2026-08-20-finding-more-like-the-favourites.md).

- [ ] **Поискать ещё Джона Мартина, ван Схрика и ван Хёйсума.**
      Из таблицы по авторам 20.08: считать надо не сколько работ автора выбрано,
      а какую долю его работ Чарли выбрал. Лоррен и Ричард Уилсон возглавляют
      список по числу (5 и 4), но это худшая доля из всех — по 2%, потому что их
      в пуле 442 из 1193. Джон Мартин — 3 работы из 23, то есть 13%, вшестеро
      чаще; ван Схрик 12.5%, ван Хёйсум 5.3%. Пул тонок именно на них.
      Осторожно с колонкой источника: кливлендских работ среди любимых 6 из 23
      при доле в пуле 3%, но это не находка — это уже отгруженные работы галереи,
      отобранные раньше и из другого пула. С этими 1193 они не соревновались.

- [ ] **Не строить ворота по коробке любимых работ — она почти ничего не режет.**
      Проверено 20.08 по просьбе Чарли: 30 названных работ (орхидея исключена им
      самим), самая тесная коробка вокруг них оставляет 307 работ из 628, то есть
      половину отбора. Каждая из двенадцати границ поодиночке оставляет больше
      80%. Причина не в замерах: любимые работы — два разных семейства (ночные
      интерьеры и дневные пейзажи), и прямоугольник вокруг двух кучек содержит всю
      середину пула. Две работы из тридцати задают 14 границ из 24.
      Что реально режет — только яркость: `luma [29, 125]` убирает 145 работ из
      321, разброс тонов ещё 100, все остальные восемь мер вместе — 32.
      Это тот же ответ, что дали два потолка 19.08, с другой стороны.
      Заодно нашлось: `cap` в `references.json` записан БЕЗ затемнения, хотя файл
      утверждает обратное — на шкале коробки восьмёрка идёт не 2.46–10.68, а
      3.67–12.82. Вердиктов это не меняет, но всякий довод вида «эталоны снизу
      упираются в 2.46» сравнивает два разных числа. Файл не правил.
      [research/2026-08-20-the-box-charlies-favourites-make.md](research/2026-08-20-the-box-charlies-favourites-make.md).

- [ ] **Исключить портреты и религиозные сюжеты прямо в запросе к музею.**
      Пул из 1451 работы отдал 18 картинок, и отсеялся он не там, где его
      настраивали: 1042 работы из 1451 — это работы, где человек в главной
      роли. Пул собирали по свету, цвету и пестроте, о сюжете не спрашивали.
      Темы в карточке Чикаго это чинят: правило «не брать, если среди тем есть
      портрет, мужчина, женщина, ребёнок, религия, святой, ню, мифология»
      даёт точность 97.8% при цене 5 годных работ из 278 (1.8%) и убирает
      треть перекачки. Запрашивать нужные темы вместо этого — померено
      и не годится: покрытие 17%.
      У Кливленда поле `subjects` в API есть, но выгрузки под рукой нет —
      проверить отдельно, вердикты по 737 работам уже готовы.
      Замеры и обе кучи ошибок: [research/2026-08-17-query-not-thresholds.md](research/2026-08-17-query-not-thresholds.md).

- [ ] **Записывать происхождение картинки в `images/gallery.json`.**
      Сейчас запись индекса — это `{file, source, added}`, а `galleryItems()`
      подставляет всем одинаковый заголовок: «Работа сообщества» или
      «Новая LLM-генерация». Для работ из открытых коллекций этого мало —
      без номера поступления нечем подтвердить, что работа действительно CC0
      (см. [LEGAL.md](LEGAL.md), раздел про открытые коллекции).
      Индекс правится руками и уже терпит записи без части полей, поэтому
      поля добавляются необязательными и старые записи не ломаются:
      `title`, `museum`, `accession`, `sourceUrl`.
      Затронуто: `gallery.js` (`galleryItems`), карточка в `public/`.

- [ ] **Выбрать источник картинок для витрины и собрать первую партию.**
      Замер по Cleveland: из 41 434 работ CC0 у 91,3 % своё разрешение уже
      3840 px и больше, то есть «редких маленьких» там почти нет. Зато
      22 920 работ — плоская графика (ткань, ковёр, гравюра, лак, мозаика).
      Проверка через TinEye: в открытом вебе те же работы лежат максимум
      500×377.
      Заодно решить, каким файлом качать: удобный JPEG обрезан до 3400 px
      и до 4K не дотягивает (нужен апскейл ≈1,13×), а полный TIFF весит
      в медиане 69 МБ.
      Подробности и цифры: [research/2026-08-16-open-access-sources.md](research/2026-08-16-open-access-sources.md).

- [ ] **Решить, на чём стоит витрина, раз «только у нас» отпало.**
      Сверка всей коллекции с Wikimedia Commons: 67,4 % работ CC0 уже лежат
      там от 2560 px, а «больше нигде нет» набирается всего на ~330 работ —
      и те отсканированы огромно, увеличивать нечего. Довод про уникальность
      снимается, доводы про отбор, кадрирование под экраны и апскейл выше
      музейного потолка — нет. Нужно выбрать, что говорим вместо него.
      Замеры и метод: [research/2026-08-16-commons-coverage.md](research/2026-08-16-commons-coverage.md).

- [ ] **Посмотреть глазами ткани Met и AIC, прежде чем их собирать.**
      У них, в отличие от Cleveland, половина работ лежит в полосе 700–2560 px —
      то есть больше их нигде и нет. Но материал в этой полосе почти весь
      текстильный, а по Cleveland ткани при просмотре забракованы: в обои
      пошли только живопись и гравюра. Возможно, у Met это плоские фрагменты
      и трафареты, а не предметы со складками — тогда вывод другой.
      Пока не посмотрели, собирать 15 тысяч записей рано.
      Замеры: [research/2026-08-16-scarce-image-sources.md](research/2026-08-16-scarce-image-sources.md).

- [ ] **Прогнать через Topaz шесть работ с коротким телефонным кадром.**
      Кадр 9:19.5 режется из плиты, и у этих шести он ниже 3120: исходник мал
      сам по себе, увеличивать нечего, кроме как моделью. Пятерым мешает потолок
      музейного скана, шестой — форма работы. Список с высотой кадра:
      vl-0166 (1536), vl-0058 (2683), vl-0057 (2790), vl-0236 (2841),
      vl-0034 (2649), vl-0179 (2947).
      Первые четыре ждут перекачки `_print` → `_full.tif`: она даёт им лишние
      пиксели и, возможно, снимает вопрос. Последние две ждать нечего —
      искали и не нашли: у vl-0034 (Мериан, Кабинет гравюр Берлина) музей
      отдаёт 1200 × 859, наш файл с Commons втрое больше; у vl-0179 (Эгедиус,
      SMK) `info.json` называет 3813 × 2947, то есть ровно то, что уже есть.
      **Сверить список с финальным набором перед прогоном.** Шесть — это те,
      кто короток сегодня, а не те, кого решено показывать: отбор ещё впереди,
      и часть из них с витрины уйдёт. Увеличенная и снятая работа — деньги
      и время, потраченные на файл, которого никто не увидит, а узнается это
      уже после. Дешевле может выйти вообще не увеличивать, а не показывать.


      **22.08: по замеру прогонять, похоже, нечего — но смотреть глазами
      за тобой.** Порог видимости растяжки — около 1,73×
      (`research/2026-08-22-japanese-museums.md`): на кусках 1:1 увеличение
      до 1,42× от эталона не отличается, а 3,47× видно сразу. Сверка списка
      с каталогом и манифестом, о которой просит абзац выше:

      | ref | кадр сейчас | до 3120 | на витрине |
      |---|---|---|---|
      | vl-0179 | 1360×2947 | 1,06× | да |
      | vl-0236 | 1311×2841 | 1,10× | да |
      | vl-0057 | 1288×2790 | 1,12× | да |
      | vl-0034 | 1223×2649 | 1,18× | `hidden` |
      | vl-0058 | 842×1824 | **1,71×** | `hidden` |
      | vl-0166 | 709×1536 | **2,03×** | `hidden` |

      Три работы, которым увеличение правда нужно, Чарли уже снял с витрины,
      а три оставшиеся стоят в 1,06–1,12×, то есть втрое ближе к эталону, чем
      порог. Топаз тут покупает ноль.

      В списке выше **исправлен vl-0058**: пункт называл 2683, а манифест
      ведёт на кадр 842×1824 — файл `...-phone-1238x2683.jpg` рядом на диске
      лежит, но он от прежнего, большего мастера, и не публикуется. Прочие
      пять высот сошлись.

      Замер — три работы, по одному самому детальному квадрату, растяжка
      Lanczos, а не Topaz. Числа тут ничего не решают: SSIM на этой лестнице
      падает сильнее всего на первом же пересчёте и дальше почти не меняется,
      то есть меряет факт растяжки, а не её размер. Поэтому и сказано —
      глазами, и это твой взгляд, не мой.
- [ ] **Двадцать две кливлендские работы всё ещё собраны с копии 3400.**
      22.08 полный скан докачан для восьми, у которых телефонный кадр не
      дотягивал до пола. Оставшиеся двадцать одна вертикальные и по телефону
      проходят — но ПЛИТА у них упёрта в 3400, значит 4K с рабочего стола не
      режется и запаса нет. Ещё ~3.4 ГБ, `node museum.mjs --only <refs>`.
      vl-0166 сюда не входит: у Кливленда полный файл 1733×1536, и это всё, что
      есть, — телефонных обоев из него не выйдет никогда.
      **22.08 одиннадцать из них вышли на витрину** по галочкам Чарли, и это
      меняет срочность: vl-0037, vl-0038, vl-0043, vl-0065, vl-0069, vl-0083,
      vl-0084, vl-0087, vl-0139, vl-0142, vl-0151 — сейчас единственные живые
      работы, у которых длинная сторона плиты упёрта ровно в 3400. Их и качать
      первыми.
      Разбор — `research/2026-08-22-the-3400-cap-and-the-tiff.md`.

- [ ] **Шестнадцать работ с листа `/edits`: сначала копия крупнее, потом Topaz.**
      21.08 Charlie отметил на `/edits` 37 работ и для каждой — какую правку она
      просит. Двадцать одна уже в каталоге и по разрешению проходит; шестнадцать
      коротки — телефонный кадр ниже пола 1320×2868. Существенное: **все 14
      новых работ короткие**, то есть добавить в витрину из отмеченного сегодня
      нечего, всё прошедшее там уже стоит.
      Список с выбранной правкой, исходником и адресом первоисточника —
      `research/chosen-edits.json`; поле `route` говорит, куда работу вести.
      Две из шестнадцати коротки не по природе: vl-0064 (2421) и vl-0060 (2732)
      — харвест взял кливлендский `_print` с потолком 3400, полный файл есть.
      **Обе с 22.08 на витрине** — Чарли их отметил, — так что докачка перестала
      быть запасом на будущее.
      Тот же потолок 22.08 сняли с восьми других кливлендских работ (Гэндзи с
      698×1513 до 1772×3840). Разбор —
      `research/2026-08-22-the-3400-cap-and-the-tiff.md`.
      **Докачаны и они: сверено 23.08** — мастера 5500×4420 и 7000×4984,
      кадры 1772×3840 и 1765×3825, обе выше пола. Модель им не нужна.
      Остальные 14 идут общим порядком отсюда: сперва копия крупнее, модель —
      на то, что останется.
      **Правка нужна не какая-нибудь.** У каждой работы своя, и после увеличения
      применить надо именно её — иначе увеличение потрачено впустую.
      Разбор — `research/2026-08-21-what-charlie-ticked.md`.
      **23.08 сделано целиком.** Копии крупнее сверены с Commons: у всех
      четырнадцати наш файл и есть оригинал, бесплатного запаса не было.
      Прогнаны через Topaz Standard V2, около $0.80. Отсмотрены на листе —
      12 работ из 16 отмечены, 4 отказаны, — и выпущены как `vl-0376…vl-0387`.
      Кливлендские vl-0060 и vl-0064 докачаны раньше и модели не требовали.
      Числа — `research/2026-08-23-upscaling-the-backlog.md`, галочки —
      `research/chosen-edits-upscaled.json`.

- [ ] **Четыре отмеченных 23.08 работы коротки, и Topaz нужен одной из них.**
      На листе избранного (`research/chosen-edits-favourites.json`) Charlie
      отметил десять работ; шесть выпущены, четыре не проходят пол 1320×2868.
      Крупнейшую копию по каждой уже искали — искать заново незачем, вот что
      нашлось и во сколько раз кадр не дотягивает:

      | работа | самый крупный найденный скан | телефонный кадр | до 2868 |
      |---|---|---|---|
      | Le Pandémonium (Мартин) | 10714×7663 **в раме** | 3537×7663 после снятия рамы | проходит |
      | Meleager and Atalanta (Уилсон) | 3028×2398 | 1106×2398 | 1,20× |
      | Forest Floor (ван Схрик) | 2118×2500 | 1154×2500 | 1,15× |
      | The Great Day of His Wrath (Мартин) | 3136×2023 | 934×2023 | 1,42× |

      **Первой увеличение не нужно вовсе** — нужна обрезка рамы, файл лежит
      в `<scratchpad>/pandemonium-avec-cadre-3.jpg`, и это отдельный пункт.
      Остальным трём копии крупнее взять негде: у Meleager четырёхтысячные
      «Meleager en Atalanta» на Commons — рейксмузейные гравюры чужих рук,
      у ван Схрика и у Мартина найденное и есть предел Commons.
      **По замеру 22.08 порог видимости растяжки — около 1,73×**
      (`research/2026-08-22-japanese-museums.md`), и все три ниже него: то есть
      обычный Lanczos отличить от исходника не выйдет, а Topaz покупает ноль.
      Правки под каждую записаны галочками: Meleager — `snap-vig` и `ceil`,
      ван Схрик — без обработки, Мартин — `bal`.
      **23.08 сделано: три увеличены и выпущены** — Meleager `vl-0386`,
      ван Схрик `vl-0385`, «Great Day of His Wrath» `vl-0383`; кадры 2214×4796,
      1846×4000 и 1867×4046, правки по новым галочкам (лист показали заново:
      старые стояли на маленьких файлах). Уточнение к таблице: у ван Схрика
      отмеченный файл — 1705×2000, а не 2118×2500, и нужда была ×1,43, а не
      ×1,15 — под тем именем на Commons лежат ДВЕ РАЗНЫЕ картины одного автора,
      сверено глазами.
      **Остался только Le Pandémonium**, и ему нужна не модель, а обрезка рамы.
      Разбор — `research/2026-08-23-upscaling-the-backlog.md`.

- [ ] **Лист `/edits` по работам, которые уже в галерее, — только ради виньетки.**
      23.08 виньетка стала обработкой генератора (`snap-vig` и двойник у
      каждого правила), и выпущены ею две работы. Остальные 95 живых
      её не видели: их выбирали в августе, когда виньетки не было, и «эта
      картина с погашенными углами лучше или хуже» им никто не показывал.
      Лист собирается тем же `scripts/research/gedits.mjs` в витринном режиме;
      нужен ему только один поворот — **пару с виньеткой давать и НЕТРОНУТОЙ
      версии тоже**, а не только приглушённым. Сейчас пары строятся по
      `dims(e)`, потому что виньетка на неприглушённой картинке читалась как
      дефект печати; Charlie 23.08: «can even vignet the originals not only
      edited versions», то есть довод остаётся доводом, но проверяется глазом,
      а не остаётся правилом. Генератор к этому готов: `none-vig` и `bal-vig`
      в `TREATMENTS` есть.
      Показывать надо ТУ ЖЕ обработку, что у работы стоит сейчас, и её же
      с виньеткой: вопрос «с углами или без», а не «выбери заново из шести», —
      иначе лист переспрашивает решённое и стоит вдвое дольше.

- [ ] **Вернуть `max-age` на год, когда правка витрины кончится.** 21.08 срок
      в `server.js` сведён с года до часа и `immutable` снят: обработка стала
      выбираться глазом, плита пересобирается под тем же именем, и год означал
      бы, что свою же правку первым не видит правящий. Посетителей нет, платить
      за их кэш нечем — но когда они появятся, цена перевесит, и год вернётся.
      Разбор — `research/2026-08-21-treatment-per-work.md`.

- [ ] **Дособрать короткие работы по копиям, а не моделью.** Увеличение —
      последнее средство, и до него есть бесплатное: у той же картины на
      Commons часто лежит скан крупнее нашего, просто под другим именем.
      Проверяется соединением по `P6243` — «цифровое воспроизведение вот этой
      работы»: два снимка одного холста несут один Q-номер, как бы ни назывались
      файлы. Сравнение названий для этого не годится и было выброшено — оно
      склеивало разные картины одного художника (семь Бирштадтов на одну
      работу Чикаго, «Jan van Huysum 001» на портрет самого ван Хёйсума).
      Из копий берётся самая крупная С ТОЙ ЖЕ пропорцией ±3%: среди файлов
      одной картины попадаются снимки её в раме на стене, они крупнее и они
      не репродукции.
      Сделано 19.08.2026 по 515 коротким работам воронки: соединились 371,
      копия крупнее нашлась у 66 (59 картин), до кадра 3120 дотянули 27.
      Скрипт: `<scratchpad>/wmsdc.mjs`.
      Осталось три хвоста, и они разные по цене:
      **144 работы без `P6243`** — про них метод не сказал ничего; это
      «неизвестно», а не «нет». Их можно соединить через Викиданные по автору
      и названию либо проставить свойство самим.
      **Музеи мимо Commons не проверены вовсе.** Met отдаёт Incapsula вместо
      ответа, запросы к SMK и Чикаго были написаны неверно (SMK не отдаёт поля
      картинки без `fields`, Чикаго склеивал по названию). Открытые API есть
      у Met, Чикаго, Кливленда, SMK, NGA, Рейксмузея, Nationalmuseum.
      **Art UK (88), Web Gallery of Art (31), Yorck Project (17) и «own work»
      (20) — тупик по устройству**, а не по случайности: это архивы
      с фиксированным маленьким размером. Крупнее у них не бывает, и время
      на них тратить незачем.
      Только после этого — Topaz, и только на то, что осталось.

## Приёмка и деньги

- [ ] **Галочка приёмки даёт одну обработку из шести, и это выбор без
      посетителя.** 22.08 она перестала считать `dim80-desat-whole` — та
      осталась на 7 работах витрины из 76, зато на 191 из 254 снятых, — и
      считает `ceil`: самую частую из отмеченных на листе 21.08 (22 отметки)
      и самую частую на витрине (19 работ). Но самый частый ответ витрины —
      не трогать вовсе (25 работ), а `bal`, `snap` и `niobe` такие же правила
      без ручных чисел и тоже могли бы стоять галочкой. Стоит ли давать
      посетителю выбор — вопрос не решённый: страница работы уже называет
      обработки словами (`TREATMENT_NAMES`), и меню из пяти строк там, где
      сейчас одна, само по себе цена. Разбор и замеры —
      `research/2026-08-22-intake-treats-like-the-ticks.md`.

- [ ] **Лицензия модели запрещает брать деньги, а страница обещает, что деньги
      будут.** С 22.08 приёмка считает в браузере посетителя моделью
      4x-ClearRealityV1 (Kim2091, CC BY-NC-SA 4.0). NC — некоммерческое
      использование. Строка «Free for now: payment and accounts come later»
      со страницы убрана 22.08, так что платное будущее сайт больше не обещает;
      пока он бесплатен, всё честно, а подпись с лицензией стоит
      на `/license#model`. В день, когда за увеличение
      начнут платить, модель придётся менять. Готовая замена без NC и без SA —
      семейство `4x-LSDIRCompact` (CC-BY-4.0), тот же класс и тот же вес,
      на наших картинках не проверено. Разбор — LEGAL.md, раздел «Модель,
      которая увеличивает картинку в браузере».
      **Уточнение от 24.08: наполовину снято.** Серверная ветка перешла
      на `4x_NMKD-Siax_200k` (WTFPL — ни NC, ни SA), и её эта мина больше
      не касается. Браузерной касается по-прежнему: Siax в браузере в двадцать
      семь раз медленнее и роняет вкладку, то есть заменить ею NC-модель там
      нельзя. Выбор в день, когда за увеличение начнут платить, сузился до
      двух: либо непроверенный `4x-LSDIRCompact` в браузере, либо браузерной
      ветки нет вовсе, а считает сервер — он теперь стоит $6.58 в месяц
      на всех (research/2026-08-24-the-model-he-picked-cannot-run-here.md).
      **Вечером того же дня ветки поменялись местами.** Кнопка ведёт на сервер,
      браузерная модель осталась запасным выходом. Мина не обезврежена — файл
      под NC лежит в репозитории и по-прежнему отдаётся тому, кто согласился
      на браузерный счёт, — но перестала лежать под главным путём. Третий
      вариант («браузерной ветки нет вовсе») стал дешевле остальных: убирать
      теперь надо запасной выход, а не то, чем сайт работает.

- [x] **Что уезжает на сервер — не удаляется ничем и названо не полностью.**
      23.08 из приёмки (`public/intake.js`, две строки) и из LEGAL.md убрано
      «deleted after 30 days»: обещание стояло в трёх местах, а кода под ним
      не было ни в одном — ни обхода каталога, ни cron, ни правила tmpfiles.
      Убрать неверную строку было быстрее, чем написать уборку, но за ней
      открылись две вещи, и обе на пути «Enlarge on our server».
      ~~**Первая: готовый файл ложится в `images/generated` навсегда.**~~
      Сделано 23.08: файла нет вовсе. Ответ `/api/upscale` — сами байты,
      имя едет заголовком `X-Filename`, `saveResult` стал `finishedImage`
      и ничего не пишет. Уборка не понадобилась: удалять нечего. Заодно
      пропала вилка, из-за которой у нас оставалась версия без размытия
      и виньетки, а скачивали — с ними. Шесть старых файлов в папке лежат
      с 16.08, это свои пробы. Цена названа в коде: результат существует
      один раз, и оборвавшийся ответ означает потерянное предсказание.
      **Вторая: картинка уезжает не только к нам.** Строка над кнопкой
      говорит «sending your picture to us» — про третью сторону не сказано
      ничего. Осталась несделанной и теперь она здесь одна: править
      `SENT_TO_US` и текст предложения в `renderOffered` (`public/intake.js`)
      вместе с LEGAL.md, чтобы третья сторона была названа во всех трёх
      местах сразу.
      **Уточнение от 24.08: сторона другая, а пункт тот же.** Replicate
      из сайта ушёл; картинка уезжает к Modal, на видеокарту, взятую
      на секунды (`upscaler.js`). Названия в текстах по-прежнему нет.
      Разница одна и в нашу пользу: у Modal картинка живёт только в памяти
      контейнера, который гаснет через десять секунд после ответа, —
      у Replicate готовый файл лежал по адресу и качался оттуда.
      **Вечером того же дня названо, и пункт закрыт.** Отправка перестала быть
      запасным выходом и стала главным путём — «to us» уже не покрывало третью
      сторону, потому что уезжает теперь у всех. Modal назван в двух местах
      из трёх, а третьего больше нет: `SENT_TO_US` (`public/intake.js`) и
      раздел `/license#model` (`pages.js`), где сказано, что это за видеокарта,
      сколько мы её держим и что у нас не остаётся ничего. Текст `renderOffered`
      про отправку молчит, потому что предлагает он теперь обратное —
      посчитать в браузере.

- [ ] **Запасной путь без видеокарты медленный: 34 с на мегапиксель.**
      Замер 22.08 в хромиуме с `--disable-gpu`: та же картинка 4.5 с на
      видеокарте и 37 с без неё. Виноват не WASM сам по себе, а один поток:
      многопоточный WASM требует SharedArrayBuffer, то есть заголовков
      COOP/COEP на странице, а с ними отвалится шрифт с fonts.googleapis.com.
      Цена вопроса — положить шрифт к себе; выигрыш по вчерашнему замеру
      втрое (10.4 с/Мп в несколько потоков).

- [ ] **Считать в Worker, а не в главном потоке.** 23.08 Чарли получил
      «Page unresponsive» с кнопкой Wait. Замер подтвердил и назвал причину:
      плитки идут через `await`, но `await` на готовом обещании — микрозадача,
      а между микрозадачами браузер не рисует и не слушает. На картинке
      600 × 900 (20 плиток) весь счёт был **одной задачей на 22.2 с**.
      Заплатка поставлена в тот же день: `breathe()` в `upscale-local.js`
      разрывает цикл макрозадачей через `MessageChannel` после каждой плитки.
      Стало 21 задача, самая долгая 1.3 с, оборотов цикла событий за время
      счёта — 673 203. Диалог этим снят: порог у Chrome на порядок выше.
      Но заплатка отдаёт ход раз в секунду, а не освобождает поток: секундные
      рывки на месте, и первый кусок — сборка сессии — не разрывается ничем,
      его компилирует сам onnxruntime. Настоящее лекарство — увезти
      `upscaleInBrowser` целиком в Worker. Переезд не тяжёлый: модуль и так
      весь на `OffscreenCanvas` и `createImageBitmap`, и то и другое в воркере
      работает. Вынести придётся чтение `<meta>` (версия рантайма и веса
      файлов) — в воркере нет DOM, — и пустить отчёт о ходе работы через
      `postMessage`.

- [ ] **`/vendor/ort` тянет в образ 133 МБ, а нужно из них 26.** Раздаётся
      весь `node_modules/onnxruntime-web/dist`, где четыре варианта wasm
      (asyncify, jsep, jspi, простой). Посетитель качает только `jsep`
      (6 МБ сжатым), остальное лежит мёртвым грузом в образе. Отбирать
      руками нечем — это чужой пакет; вариант — копировать нужные файлы
      на сборке.

- [x] ~~Проверить маршрутизатор «бесплатно или за деньги» на невиданных
      картинках.~~ Проверен 22.08 на тринадцати новых — **не работает**.
      5 угадываний из 9 там, где выбор имел значение; занятость 28.7 % там, где
      бесплатной хватило, и 29.3 % там, где нет. Порога, который их делит, нет:
      самый занятый кадр прошёл бесплатно, самый гладкий потребовал платного.
      Оговорка «ошибка тут дешёвая» оказалась половиной правды: промахи в обе
      стороны идут поровну, 2 и 2, и один из них — мягкие обои, а не пятак.
      `research/2026-08-22-busyness-does-not-say-who-needs-paying.md`.

- [ ] **Решить, чем заменить маршрутизатор.** Два пути: платить за всех, у кого
      не нашлось копии крупнее, или отдавать бесплатный счёт в браузере сразу и
      предлагать доплатить тому, кто скажет «мягко». Второе снимает и вопрос
      порога, и вопрос очереди на нашей коробке, но требует места в интерфейсе,
      где человек это скажет. Мерить переходы яркости третий раз не стоит: так
      уже умерли мера мелкости и мера занятости.

- [ ] **Переиграть сравнения моделей с включёнными лицами у Topaz.**
      `face_enhancement` выключен по умолчанию, и весь день 22.08 мы звали
      Topaz без него. Ничья 4:4 с Real-ESRGAN недействительна. Заодно прогнать
      Topaz ×4 с ужатием ($0.80 за шестнадцать): бесплатная ветка иначе не
      умеет, а платная, возможно, от этого выиграет.

- [ ] **Если перейдём на свою модель у себя на машине — пересмотреть ворота
      на `/api/upscale`.** С 22.08 маршрут считает вызовы (`limits.js`):
      пять в час с браузера, десять в сутки с адреса, пятьдесят в сутки на всех.
      Все три числа держатся на одном — за каждый вызов платим мы, Replicate
      берёт за штуку. Со своей моделью эта причина исчезает, а на её место
      встаёт другая: не счёт, а видеокарта — сколько задач она тянет разом
      и как долго. Тогда правильный ответ — скорее очередь («одна задача
      за раз, остальные ждут»), чем потолок в сутки, а сам потолок можно
      поднимать вольно. Ворота адреса и куки при этом остаются: они не про
      деньги, а про то, чтобы один посетитель не занял машину целиком.
      Разбор — `research/2026-08-22-rate-limits.md`.
      **Уточнение от 22.08:** «своя модель» получилась не у нас на машине, а
      в браузере посетителя — у боевой коробки видеокарты нет вовсе, а модель
      весит 1.9 МБ и считает у него. Тогда исчезает и вторая причина: ни счёта,
      ни очереди, ни нашей видеокарты. Ворота нужны только тем вызовам, что
      уходят в Topaz за деньги.
      **Сделано в тот же день:** приёмка считает в браузере и на `/api/upscale`
      больше не ходит — маршрут остался запасным путём для браузеров, которые
      так не умеют. То есть ворота теперь охраняют именно то, что стоит денег,
      и трогать их числа не нужно. Пункт закрывать рано: сам счётчик ни разу
      не видел настоящего отказа в бою.

- [ ] **Суточный потолок обнуляется при перезапуске.** Счётчики в памяти —
      это выбрано сознательно (нет файла, нет записи на каждый вызов), но
      деплой в разгар дня выдаёт следующие полсотни. Когда это начнёт стоить
      заметных денег, счётчик «сервера» переедет в файл; куки и адрес могут
      остаться в памяти.

- [ ] **Страница приёмки молчит о нормах.** Посетитель узнаёт про пять в час
      только из отказа. Строка под кнопкой на измеренном файле теперь пуста —
      если норму называть вслух, то там.

- [ ] **Прозрачность в браузере возвращается на чёрной плашке.** `runTile`
      кормит модель тремя каналами и ставит выходу `alpha = 255`
      (`public/upscale-local.js`), а `getImageData` отдаёт прозрачному
      пикселю `(0,0,0,0)` — то есть модель видит чёрное. PNG приёмка
      принимает. На сервере этого нет: `treatment.js` уводит альфу в сторону
      и возвращает через `joinChannel`, и комментарий там называет ровно этот
      случай. Значит, один и тот же файл даёт два разных ответа в зависимости
      от того, где он посчитался. Зачем чинить: логотип на прозрачном фоне —
      не выдуманный случай, а тихая порча.

- [ ] **Потолок площади холста — одно число на все браузеры.** 22.08 в
      `upscale-local.js` появился `MAX_AREA = 16 777 216`: это предел Safari,
      и до него теперь ужимаются все, хотя у Chrome он в шестнадцать раз
      выше. Иначе нельзя: строка на странице обязана назвать размер до того,
      как холст создан, а померить предел можно только выделив под него
      память. Цена — «×4» от крупного исходника отдаёт 16,8 Mpx вместо 44.
      Для телефонных обоев (1440 × 3120 — это 4,5 Mpx) с запасом хватает,
      поэтому спешки нет. Если чинить, то пробой снизу вверх и не выше
      запрошенного, с перерисовкой обещанного размера.

- [ ] **Обещанный размер и серверный расчёт разошлись.** `resultLongestSide`
      **Уточнение от 24.08: причина вернулась, но подешевела в одиннадцать
      раз.** Своя модель нашлась не на нашей машине и не в браузере, а на
      чужой видеокарте, взятой на секунды: вызов стоит $0.0044 вместо
      $0.05, и те же пятьдесят в сутки — $6.58 в месяц вместо $75. То есть
      потолок перестал быть границей расхода. Очередь, о которой говорит
      этот пункт, тоже не нужна: видеокарта не наша, и «сколько задач она
      тянет разом» решает `max_containers=2` в `service/upscale_modal.py`.
      Остаётся ровно то, ради чего ворота и стоят, — чтобы один посетитель
      не съел общий счёт; числа можно поднимать, упрутся они не в деньги.
      **Вечером того же дня числа стали видны посетителю, и это первое, что
      надо померить.** Пока считал браузер, ворота не встречал никто: маршрут
      был запасным выходом для редкого браузера. Теперь через него идут все,
      кто нажал «Increase size», и пять картинок в час с браузера — это ровно
      столько попыток, сколько человек делает, подбирая обои. Отказ при этом
      не тупик: страница предлагает посчитать в браузере (`renderOffered`),
      то есть ворота из «нельзя» стали «медленнее и бесплатно». Числа не
      тронуты сознательно — сперва надо увидеть, во что упирается живой
      посетитель, а не угадать.
      в `upscale-local.js` держит потолки стороны и площади, а
      `targetLongestSideFor` в server.js и `saveResult` — нет. Пока это видно
      только на предложении посчитать у нас: кнопка там называет своё число
      (`serverSize` в `public/intake.js`), а не то же самое. Честно, но два
      правила вместо одного. Зачем чинить: расходиться дальше они будут молча.

- [ ] **Галочка «приглушить» держит вчетверо больше памяти, чем весит
      результат.** На 49,6 Mpx это `getImageData` + три буфера подряд, около
      650 МБ живыми, и всё на главном потоке (`public/treat-local.js`). На
      телефоне это падение вкладки. Считать бы плитками или переиспользовать
      один буфер. Потолок площади 22.08 срезал худший случай, но не задачу.

- [ ] **Адрес показанной картинки отзывается раньше, чем загрузилась
      замена.** `restore` в `public/intake.js` зовёт `revokeObjectURL` на
      старом адресе, на который `els.picture.src` ещё смотрит, и только потом
      грузит готовое. Если загрузка не удалась, кадр остаётся с мёртвым `src`
      и уже не кликается: `role`/`tabindex` с него сняты при первом показе.
      Отзывать надо после `showPicture`, а не до.

## Сайты вокруг проекта

- [ ] **Несколько HTML-страниц под GEO** — поиск агентами и LLM работает не так,
      как обычный поисковый: нужен текст, который читается без JS.

- [ ] **Страница со статусом задач и результатами экспериментов.**
      Доступна всем участникам, чтобы прогресс не приходилось спрашивать.

- [ ] **Одностраничное сравнение с конкурентами.**

## Домен и имя

Имя выбрано и домен куплен: **tessarum.com**. Осталось довести до сайта.

- [ ] **Настроить DNS на tessarum.com.**
      Домен зарегистрирован, но никуда не указывает — до этого сайт живёт
      только по служебному адресу хостинга.

- [ ] **Переименовать продукт из `Vellum` в `Tessarum`.**
      `vellum` было рабочим именем из прототипов, теперь у него есть замена.
      Пока имена разъезжаются, домен и сайт называются по-разному — это первое,
      что видит посетитель.
      Затронуто: `pages.js` (`SITE_NAME`, тексты страницы лицензии),
      `works.js` (`DEFAULT_LICENSE`, `Vellum License`), `gallery.js`
      (`source: 'vellum'` в записях индекса), `public/record.js`
      (`ACCESSION_PREFIX = 'VL·'` — префикс идёт от имени и меняется вместе
      с ним). Прототипы в `prototype-*.html` и записи в `research/` —
      история, их не трогаем.

---

Задачи из разделов «Сайты вокруг проекта» и «Домен» перенесены сюда из
`research/plan.md`. Пока файл жив, список задач существует в двух местах —
это ровно тот случай, про который в [AGENTS.md](AGENTS.md) написано «одно имя
на одну сущность». `research/plan.md` стоит удалить.
