# Подготовка проекта к дальнейшей работе агентов — выполнено

Дата: 16 августа 2026. Ветка `maintainability-cleanup`.
Предложение и обоснование: [2026-08-16-maintainability-cleanup.md](2026-08-16-maintainability-cleanup.md).

## Коротко

Выполнены семь коммитов из плана. Поведение сайта не изменилось нигде,
кроме двух мест, названных ниже. Каждый коммит проверяется отдельно.

| Коммит    | Что                                                    |
| --------- | ------------------------------------------------------ |
| `7b82799` | Исправления в `server.js`: мёртвый код, имена, ошибки  |
| `ba6c794` | Prettier и `.editorconfig`                             |
| `bb56ed6` | Форматирование: `public/` и `server.js` развёрнуты      |
| `c45c120` | `yarn verify` и `.git-blame-ignore-revs`               |
| `f63a3b6` | `public/app.js`: перекрытые глобальные имена, дубль     |
| `63ce6cb` | Изображения сняты с учёта, каталоги остались            |
| `cbb1c9a` | Пустой `public/ CODEX.md`, имя пакета                   |
| `333c3b7` | `AGENTS.md` переписан                                   |

## Цель

Сделать репозиторий пригодным для того, чтобы следующий агент писал в нём
код, не ухудшая его. Агент выбирает стиль, глядя на соседний код, и верит
тому, что написано в `AGENTS.md`. Значит, до появления нового кода нужно,
чтобы соседний код был читаемым, а инструкция — правдивой.

## Принятые решения

**Форматирование отдельным коммитом.** `bb56ed6` — чистый вывод
`yarn format`, ни одной правки по смыслу. Его SHA лежит в
`.git-blame-ignore-revs`, поэтому `git blame` продолжает показывать
настоящего автора строк. Включается один раз на машину:

```bash
git config blame.ignoreRevsFile .git-blame-ignore-revs
```

**Команда проверки называется `verify`, а не `check`.** `yarn check` —
встроенная команда yarn 1: она перекрывает одноимённый скрипт и завершается
успешно, ничего не проверив. Это та же ловушка, из-за которой в `AGENTS.md`
оказался несуществующий `yarn build`.

**Ошибки разделены на предназначенные посетителю и все остальные.**
Введён класс `HttpError(status, message)`. Наружу уходит только его текст;
любая другая ошибка превращается в «Внутренняя ошибка сервера» и пишется
в лог. Маршруты больше не отвечают на ошибки сами — все зовут `next(error)`.

**Изображения сняты с учёта, каталоги оставлены** через `.gitkeep`.
Фотографии живут на сервере.

**Имя пакета — нейтральное `upscaler`.** `topaz-x2-upscaler` называл сервис,
которого в коде давно нет. Название продукта выбирается сейчас отдельно,
и здесь его лучше не опережать.

## Изменения по файлам

### `server.js`

- убран `OUTPUTS_DIR` — синоним `GENERATED_DIR`; убран дублирующий маршрут
  `/outputs`, каталог `outputs/` в корне и его правила в `.gitignore`;
- в `MODELS` добавлено поле `slug`, имя файла результата строится из него;
- удалены `model.version`, запасной `endpoint`, проверка
  `'Only images are allowed'` — ни одна из веток не выполнялась;
- `getTargetLongestSide` и `getRealEsrganOptions` заменены на
  `readLongestSide` (читает размеры один раз) и две чистые функции
  `targetLongestSideFor`, `realEsrganScale`;
- добавлен `HttpError`, `/api/upscale` переведён на `next(error)`,
  обработчик в конце файла отдаёт 404 на отсутствующий файл вместо 500.

### `public/app.js`

- `status` → `statusLine`, `open` → `openLightbox`, `shut` →
  `closeLightbox`, `close` → `boxClose`: три имени верхнего уровня
  перекрывали свойства `window`;
- текст кнопки собирался в двух местах; теперь `updateSubmitLabel`
  и `currentOutputSize` — одно правило в одном месте.

### Инфраструктура

`.prettierrc.json`, `.prettierignore`, `.editorconfig`,
`.git-blame-ignore-revs`; в `package.json` скрипты `format` и `verify`,
`test` вызывает `verify`. `AGENTS.md` переписан.

## Что изменилось в поведении

Два изменения, оба намеренные.

1. **Ссылка на результат.** Было `/outputs/<файл>`, стало
   `/images/generated/<файл>` — тот же каталог под одним именем. Фронтенд
   берёт ссылку из ответа API, правок в `public/` не потребовалось. Внешние
   ссылки на `/outputs/…`, если они где-то сохранены, перестанут работать;
   домена и индексации пока нет, поэтому момент для смены удачный.
2. **Текст ошибки для посетителя.** Раньше `/api/upscale` показывал
   `error.message` как есть — включая «Добавьте действительный
   REPLICATE_API_TOKEN в файл .env». Теперь такие сообщения идут в лог.

Отдельно: после свежего `git clone` коллекция будет пуста, пока фотографии
не положены в `images/storage` на сервере. Страница отрисует пустую сетку,
ошибки не будет.

## Результаты проверок

`yarn verify` — код 0, «All matched files use Prettier code style!».

Сервер на `PORT=3999`, реальные ответы:

```
GET  /                           200
GET  /api/gallery                200
POST /api/gallery/refresh        200
POST /api/upscale (без файла)    400
POST /api/gallery/share/x.txt    400
POST /api/gallery/share/no.jpg   404
GET  /images/generated/no.jpg    404
GET  /outputs/anything           404   (маршрут удалён)
```

Проверка, что токен больше не виден посетителю — запрос с картинкой при
незаполненном `.env`:

```
ответ:  {"error":"Внутренняя ошибка сервера."}
в логе: Error: Добавьте действительный REPLICATE_API_TOKEN в файл .env.
            at apiHeaders (server.js:123:11)
```

Проверка, что форматирование не изменило вёрстку: страница отрисована
в браузере до и после `bb56ed6` на одном сервере; сравнивались
`document.body.innerText` и прямоугольники всех 94 элементов в обоих
режимах (коллекция и студия) — совпадают полностью. Значение `#prompt`
не изменилось.

Проверка `public/app.js` в браузере: `window.open`, `window.close`,
`window.status` снова принадлежат браузеру; метка кнопки проходит
×2 → 4K → (портрет) 1K → ×4 → (обычный) 4K, то есть каждый режим помнит
свой размер; `#active-model` переключается `real_esrgan` ⇄ `nano_banana`;
лайтбокс открывается по клику на карточку, фокус уходит на крестик,
прокрутка `body` блокируется, закрывается по Escape и по кнопке.

Проверка `git blame` после включения `blame.ignoreRevsFile`: строки
`MODELS` в `server.js` числятся за alivkon и коммитом `fa0a102`,
а не за форматированием.

## Ограничения

Реальные вызовы Replicate не выполнялись — они платные. Успешный путь
`/api/upscale` (создание задачи, ожидание, скачивание, сохранение) проверен
только до места, где требуется токен. Имя файла результата с `slug` модели
и запись в `images/generated` на живом вызове не проверялись.

`.git` по-прежнему весит 68 МБ: снятие с учёта не удаляет объекты из
истории. Уменьшить размер может только перезапись истории
(`git filter-repo` или BFG), а она ломает все существующие клоны —
это решение владельца репозитория.

Не сделано намеренно, вынесено в предложение как требующее решения:
переход коллекции на файл-индекс вместо сортировки по времени файла;
ограничения на платный `/api/upscale`; запрос длиной до 10 минут против
таймаутов прокси в 30–100 секунд; общий шаблон для HTML-страниц.
