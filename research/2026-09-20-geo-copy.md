# GEO copy: пять фраз пущены в код

20 сентября 2026

## Цель

Заявка `research/2026-09-20-geo-copy-draft.md` описала пять мест, где
цитируемое предложение отвечает на то, что спрашивают у ChatGPT и похожих
ассистентов. Черновик текста не трогал; Charlie одобрил пять формулировок,
и эта запись — про их установку в код.

Жёсткое правило заявки: ни одного тире (—) в новой копии. Только запятые,
точки, двоеточия.

## Пять строк

1. **`DESCRIPTION`** (`pages.js`, главная и обходные страницы через `layout`):
   «Real museum paintings and engravings, not AI-generated, as free phone
   wallpapers at 2160 × 3840. Each comes cropped for the screen and in
   a dimmed version so icons stay readable. No sign-up, no watermark. Make
   your own from a picture of your own, up to 4× bigger.» Проверено по коду:
   «no watermark» уже используется в `collections.js` и `COLLECTIONS.md` как
   подтверждённое утверждение; «engravings» и «2160 × 3840» были в прежнем
   тексте.

2. **Подпись у галочки «Dimmed»** (`dimmedBox()`): «Dimmed is the painting
   made darker and less colourful, so your icons and clock stay easy to
   read. Switch it off for the plain scan.» Не отдельным абзацем и не новым
   CSS-классом — вторая строка внутри `.options__text`, тем же
   `.options__fine`, что уже несёт «no preview» у «Increase size» в приёмке.
   `:has(.options__fine)` уже переключает выравнивание квадрата с середины
   на верх — писать второе правило не пришлось. Работы с `treatment: none`
   галочки не получают, и подписи у них поэтому тоже нет — проверено вживую
   на `aivazovsky-the-ninth-wave-desktop-wallpaper`.

3. **`/license`, первый абзац**: «These are scans of real paintings and
   drawings held by museums, not AI-generated images; cropping and
   darkening them for a phone screen is ordinary image processing, done by
   fixed rules, not by a generator.» Заявленное Charlie «done the same way
   on every file» смягчено: по AGENTS.md у поля `treatment` три значения
   (`dim`, `ceil`, `none`), и «тем же способом» для всех файлов неверно.
   «Done by fixed rules, not by a generator» верно при любом из трёх правил.

4. **`/license`, перед «Works from open collections»**: «Using a painting
   from this collection as your wallpaper is free and needs no permission.
   Most are public domain or CC0, and the few under CC BY ask for credit
   only if you share the image.» Формулировка одобрена как есть, без правок:
   не противоречит секции `#cc-by` — та же страница просит credit «when you
   take» файл, и требование срабатывает практически при публикации,
   а не при личном использовании.

5. **`workPage()`, meta description**: шаблон `<Title> by <Artist> (<year>),
   a free <w> × <h> phone wallpaper from <credit>.» встал перед прежним
   текстом (`item.alt`, размер, тип файла, вес, «Free download, no
   sign-up.» — всё это осталось, только новая фраза говорит первой). Поля
   считаны по `catalogue/*.json` (411 записей, без `order.json`):
   - `item.provenance` отсутствует у 60 записей (свои работы) — для них
     фразы нет вовсе, как нет и у них байлайна на самой странице;
   - creator помечен `unknown` у 77 из 351 записей с provenance — «by
     …» опускается;
   - `date` отсутствует у 29 — скобки с годом опускаются;
   - `credit` отсутствует у 3 — «from …» опускается.
   Других отсутствующих полей в данных не нашлось, других веток поэтому
   не заводилось.

## Файлы

- `pages.js` — все пять правок; `git diff` даёт только эти строки плюс
  Russian-комментарии рядом с ними, добавленные в плотности соседей.
- Никакой новый CSS не понадобился: `.options__fine` в `public/styles.css`
  уже существовал для того же назначения.

## Проверки

`yarn verify` — код 0:

```
каталог: 410 записей, 410 адресов, порядок задан
темы: nihonga — 16, moody-landscape — 14, dark-academia — 43
журнал запросов: четырнадцать проверок пройдены
счётчик вызовов: восемь проверок пройдены
список рассылки: 20 проверок пройдены
уход тона по каналам, уровней: у модели  -5.95  -2.98  -3.98 | после пересадки  -0.05   0.04   0.01
пересадка тона: тон вернулся, профиль на месте
Checking formatting...
All matched files use Prettier code style!
```

Сервер поднят на `PORT=3198` (не мешал боевому на 3000), проверены `curl`ом:

- `/` — новая `DESCRIPTION` в `<meta name="description">`, подтверждено.
- `/license` — оба новых абзаца на месте, третьим и предпоследним перед
  «Works from open collections» соответственно.
- `/collection/dark-academia` — 200.
- Три страницы работы: `a-forest-with-apollo-and-daphne-bertin-iphone-wallpaper`
  (обычная, все поля есть — description начинается «A Forest with Apollo and
  Daphne by Jean-Victor Bertin (1810), a free 2160 × 3840 phone wallpaper
  from Cleveland Museum of Art, 1975.77.», подпись у «Dimmed» на странице
  есть), `aivazovsky-the-ninth-wave-desktop-wallpaper` (`treatment: none` —
  без чекбокса и без подписи, description без «by» опущен не был, поле
  creator у неё есть), `after-kalf-still-life-nautilus-iphone-wallpaper`
  (creator `unknown` — description начинается «Still Life with Nautilus Cup
  (1665–1670), a free …» без «by»); отдельно проверен пропуск года на
  `crow-in-flight-on-gold-iphone-wallpaper` («Crow in flight, a free …» без
  скобок).
- `grep` по добавленным строкам на «—» — ни одного вхождения в английской
  копии; тире остались только в русских комментариях рядом (это разрешено,
  правило — про копию для посетителя).

## Ограничения

- Скриншот на 360 px через chrome-инструменты не снимался: проверка `curl`
  и разбор CSS (`.options__fine`, `:has()`) показали, что верстка уже несёт
  этот случай — второй строке в лейбле, — и отдельный визуальный прогон
  решено пропустить.
- Пункт 4 (`/license`, легальность) не сверялся построчно юристом — формула
  одобрена Charlie заранее, только проверена на непротиворечие остальному
  тексту страницы.

## Правка от того же дня: «from …» врал на части записей

Пункт 5 выше проверил только отсутствие `credit`, а не то, что в нём
написано. Оказалось: `credit` — не всегда держатель работы. У vl-0392
(ворона на золоте) он — `Photograph by Sailko, CC BY 3.0, via Wikimedia
Commons`, подпись фотографа, и фраза выходила «a free … phone wallpaper
from Photograph by Sailko, CC BY 3.0, via Wikimedia Commons» — бессмыслица.

### Перепись `credit` по всем 410 записям (без `order.json`)

Группировка по форме строки, счёт отдельно по живым (не `hidden`) записям —
скрытые в счёт не идут, как договорено:

| форма | всего | живых |
|---|---|---|
| музей + инвентарный номер («Cleveland Museum of Art, 1960.80» и т.п.) | 182 | большинство |
| голое «Wikimedia Commons» (без музея — просто витрина) | 122 | 53 |
| название музея без номера («Tokyo National Museum» и т.п.) | 25 | 25 |
| поля нет вовсе | 3 | 2 |
| «Photograph by …» (подпись фотографа) | 1 | 1 |

Полей, которые называли бы держателя отдельно от `credit` (`source`,
`institution`, `holder`, `museum`), в `provenance` нет — проверены все ключи,
встречающиеся хоть раз: `creator`, `date`, `work`, `credit`, `page`,
`creatorKind`. Полевого решения поэтому нет, и правило неизбежно смотрит
в строку `credit`.

Из 351 живой записи с `provenance` под старым кодом (`credit ? \` from
${credit}\` : ''`) фраза «from …» врала в 54 случаях: 53 «Wikimedia Commons»
и 1 «Photograph by …». Остальные, даже длинные («Musée Fabre, Montpellier,
2017.27.1», «Adachi Museum of Art, via Google Art Project and Wikimedia
Commons»), называют держателя честно и не тронуты.

### Правило

`pages.js`, `workPage()`: `credit` идёт во фразу, только если это не голое
«Wikimedia Commons» и не начинается с «Photograph by» (без учёта регистра).
Оба случая — не название держателя, а название витрины или подпись
фотографа; фраза без «from …» при этом читается нормально сама по себе —
«Crow in flight, a free 2552 × 4536 phone wallpaper.» Комментарий над
`geoLine` переписан: было устаревшее «шестьдесят» записей без `provenance`
(пересчёт сегодня даёт 59 — число менялось вместе с каталогом уже после
исходной правки), добавлен разбор новой ветки.

Матчинг по строке — не поле, и это ровно тот случай, для которого AGENTS.md
просит объяснить: полевого способа нет, а правило одно и явное, а не набор
случайных слов.

### Проверка

`yarn verify` — код 0 (та же строка каталога: 410 записей). Сервер поднят на
`PORT=3197` (не боевой), `curl /w/<slug>`, meta description:

- `crow-in-flight-on-gold-iphone-wallpaper` (`credit` = «Photograph by
  Sailko…») — «Crow in flight, a free 2552 × 4536 phone wallpaper. …» —
  «from …» ушло;
- `vase-of-flowers-huysum-iphone-wallpaper` и
  `flowers-in-an-urn-huysum-iphone-wallpaper` (`credit` = голое «Wikimedia
  Commons») — обе фразы кончаются на «phone wallpaper.» без «from …»;
- `silver-wine-jug-ham-and-fruit-dark-still-life-iphone-wallpaper` и
  `view-of-lake-pepin-minnesota-river-landscape-desktop-wallpaper`
  (`credit` = «Cleveland Museum of Art, …») — «from Cleveland Museum of Art,
  1960.80.» и «from Cleveland Museum of Art, 1947.198.» остались на месте.

Сервер остановлен после проверки, порт 3000 не трогали.

### Файлы

`pages.js` — комментарий и три строки (`holder`, `fromCredit`, `geoLine`)
в `workPage()`; `gallery.js` и два незакоммиченных абзаца в `pages.js`
(`intakePage`) не тронуты — они не по этой задаче.
