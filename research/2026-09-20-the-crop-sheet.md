# Кадр партии vl-0399…vl-0488: старый лист, две строки патча

Кадр, снятый вчера на листе обработки, — прикидка, и так и был задуман. Лист
обработки спрашивает «нужна ли картина и каким правилом», телефонное окно
показано там затем, чтобы на этот вопрос было чем ответить: картина, у которой
в окне нет ничего, не нужна ни с каким правилом. Кадр ставится своим листом —
`crop-positioner`, — и вчерашние 45 чисел ему начальное положение.

## Что сделано

`scripts/research/crop-positioner.mjs` — две правки, обе точечные.

1. **`--only` не требует карточки в `catalogue/`.** Тот же патч, что получили
   `treat-sheet.mjs` и вчера `crop-ruler.mjs`: ref, которого нет в каталоге,
   берётся прямо из `museum-works.json`, заголовок — полем `name`. Без него лист
   по 45 работам выходил пустым, потому что кадр по пайплайну ставят ДО каталога.
2. **Копия плиты берётся 1920, а не 480** (`THUMB_AT`). 480 — ровно ширина
   самого широкого проёма в CSS-пикселях: 1:1 на столе и втрое мельче нужного
   на телефоне. Число названо последним из размеров копий (240/480/960/1920),
   а не серединой: «ближайшая» при равном расстоянии берёт первую, и 1440 молча
   выбирало бы 960.

Лист собран по 45 оставленным работам: `research/crop-positioner-only.html`,
45 карточек. Картинки лист берёт с витрины, так что нужен поднятый `yarn start`.

## Тупик, записанный, чтобы не повторяли

Сначала рисующая половина листа была переписана: плитки пеклись из мастера,
как в листе обработки, — с `trim`, балансом и правилом `treatment`, окно 1080 px,
три вкладки, файлы рядом с листом. Работало и было проверено (размеры окон
совпали с выпущенными кадрами байт в байт у трёх работ на всех трёх пропорциях;
19.2 МБ плиток на 45 работ). Но это была замена работающего инструмента там,
где хватало двух строк, и переписанное выброшено. Copy лежит вне репозитория,
в рабочем каталоге сессии.

Что из той попытки стоит помнить, если к ней вернутся:

- 9:16 на листе должен идти за телефонным окном ЖИВО, включая только что
  сдвинутое: `frameCrop` отдаёт `tall` правило `phone`, и лист, показывающий
  9:16 по старому числу, показывает кадр, которого не выйдет. Нынешний лист
  этого не делает — вкладка `tall` открывается по записанному правилу;
- обратные кавычки в русском комментарии ВНУТРИ выдаваемого клиентского JS
  закрывают внешний шаблонный литерал; `prettier` файл при этом разбирает,
  а `node` — нет. Второй раз за два дня;
- отметка «это была езда, а не тап» нужна только там, где по плитке ещё и
  выбирают тапом. На чистом листе кадра она глотает первое нажатие на ⊕
  после каждого перетаскивания.

## Проверки

- Лист по 45: все 45 карточек на месте, у всех есть запись в манифесте.
  Проём открылся на записанном правиле — vl-0399 `left 824px / 2169px`
  (в файле 823), vl-0440 `left 1272px / 1272px` (упёрт в правый край, так
  и записано), vl-0485 на 32.
- Единица расхождения — округление листа: сдвиг он считает долями ширины
  уменьшенной копии, и один её пиксель на 1920 — это 1.8 пикселя плиты.
  На 480 было 7. Ошибка ложится только на записанное число, и только если
  работу трогали.
- `npx prettier --check scripts/research/crop-positioner.mjs` — чисто.
- `yarn verify` — код 0.

## Ответы Charlie: 41 кадр из 45

Лист пройден, вернулось 55 строк на 41 работу: у одиннадцати кадр записан
дважды или трижды. Сначала их применили по правилу линейки рамки — «побеждает
последняя», — и это оказалось неверно: кнопка `record` на этом листе добавляет
строку, а не заменяет её, и повторная запись означает второй ВАРИАНТ кадра,
а не исправление первого. Берутся все, по порядку; `v1` уходит
в `museum-works.json`, остальные лежат строками в `to-crop-positions.md`.
Свёрнут только буквальный повтор (vl-0457 и vl-0460 — одно и то же число
подряд, дважды нажатая кнопка). Правило записано в шапку
`to-crop-positions.md` и в `ADDING.md`; одиннадцать работ переприменены
и пересобраны.

Все 41 попали внутрь хода, ни одного клампа. Трое (vl-0437, vl-0442, vl-0446)
упёрты в левый край — это ответ, а не ноль по умолчанию.

Попутно нашёлся пропущенный багет: у vl-0407 линейка записала только боковые
поля (`left 554`, `right 533`), а это литография с бумажным полем и подписью
под изображением. Домеряно `top 554`, `bottom 805`; плита 3883×3840 → 3883×2481.
`crop.left` при этом не переписывался: срез сверху и снизу горизонтальный
отсчёт не двигает.

Проверка: `verify-frames.mjs` по этим 41 — 123 кадра, худшее расхождение
0.26 при пороге 4 (шум JPEG ≈2, чужой кадр ≈30). То есть все три кадра каждой
работы вырезаны ровно там, где записано.

## Дубликаты

Charlie назвал дубликатами vl-0427, vl-0436 и vl-0441 и кадр им не ставил.
Первые пары нашлись по имени — у обеих второй файл слуган с суффиксом `-2-`,
— и подтвердились картинкой. Но этого способа не хватило.

**Имена ловят не всё.** vl-0451 (`ottesen-blomsterstykke-med-roser`) оказался
той же картиной, что vl-0443 (`ottesen-still-life-with`) — разница 1.8, ниже
шума пережатия, — а по именам они не пересекаются ничем. Поэтому вся
коллекция сверена попарно по миниатюрам 64×64: 384 мастера, 73 536 пар.
Мастеров vl-0376…vl-0387 на диске нет, они в сверку не попали; из партии
0399…0488 — ни одного пропуска.

Среди 45 оставленных работ вышло три гнезда, и больше ничего ближе 13:

| гнездо | работы | разница | Мпикс · лапласиан на одинаковом куске |
|--------|--------|---------|----------------------------------------|
| Кальф, «Pragtstilleben med Holbeinskål» (KMS1531) | vl-0423, vl-0436, vl-0441 | 0.2 / 11.1 / 11.2 | 46.4 · 14.19 · · 15.2 · 10.39 · · 46.4 · 14.43 |
| Оттесен, розы и земляника | vl-0443, vl-0451 | 1.8 | 7.3 · 6.95 · · 37.4 · 8.57 |
| Гюден, «Gezicht op een rotsachtige» | vl-0408, vl-0427 | 7.9 | 11.1 · 6.60 · · 5.0 · 7.06 |

Кальф пришёл тремя файлами Викимедиа: Google Art Project (vl-0423) и два
из SMK (vl-0436 мельче, vl-0441 тот же размер, что у Google, но втрое
меньше сжат). По детали vl-0441 и vl-0423 равны, vl-0436 заметно мягче.

У Гюдена лапласиан у мелкого скана выше, чем у крупного, — это не деталь,
а резкость, накрученная в самом файле: пикселей у него вдвое меньше.
Мера различает сканы одной работы, но не заменяет глаз.

**Выбор сделан глазами: «примерно одинаковые, берите любой».** Лист для
выбора — `scripts/research/duplicates-sheet.mjs`, он кладёт рядом целую работу
и кусок из одного и того же места картины в масштабе 1:1; во сколько раз один
кусок больше другого, во столько раз больше в нём и пикселей. Раз разницы
не видно, оставлена по каждому гнезду самая крупная плита: vl-0423 (3840×4632),
vl-0451 (3740×4828) и vl-0408 (3710×2980). Удалены vl-0436, vl-0441, vl-0443
и vl-0427 — 88 файлов, 0.09 ГБ.

У Оттесена это сменило ref: кадр стоял на vl-0443, но его плита 2229×2928
против 3740×4828 у vl-0451. Переносить кадр не пришлось — свой `crop`
и `treatment` записаны у обоих, лист кадрировки прошли оба файла.

## Отказанные убраны

45 работ, не отмеченных на листе обработки, удалены целиком: записи
из `museum-works.json`, записи из манифеста, плиты, кадры и мастера —
1161 файл, 1.13 ГБ. Перед удалением партия закоммичена и отправлена
(`wallpaper-gen ae6a505`): адреса источников остались в истории, иначе
картину, отвергнутую однажды, некому было бы узнать при следующем сборе.

Осталось 347 работ, столько же записей в манифесте; ни одной записи
без файла и ни одного файла без записи. Строки отказанных в
`research/to-crop-positions.md` и `to-crop-frames.md` не тронуты: это
исторические записи, и обмер, сделанный над удалённой работой, — тоже
результат.

## Каталог: 41 карточка

Черновики карточек написаны подагентом и лежат в
`research/2026-09-20-catalogue-drafts.json` — там же остались поля `needs`,
которых в карточке нет: у десяти работ не подтверждён первоисточником
инвентарный номер или страница музея (`vl-0407`, `0412`, `0437`, `0442`,
`0447`, `0455`, `0464`, `0472`, `0474`, `0485`). Ничего не выдумано:
где номер не нашёлся, в `credit` стоит музей или Викисклад.

Четыре работы названы не тем автором — и это пришло из имени файла
на Викискладе, а не из карточки:

- vl-0437 — не Кальф, а Виллем ван Алст (подтверждено API самого SMK);
- vl-0485 — не Кальф, а анонимная копия с него (NGA и Викиданные);
- vl-0464 — не Коул, а копия с него около 1863 года, через 15 лет после
  его смерти;
- vl-0474 — одна из двух «Бурь у мыса Айя», и путать её с версией 1875 года
  в Русском музее нельзя.

Двум работам сменили `name` в `museum-works.json` и пересобрали их:
`kalf-van-…` → `van-aelst-stilleben-maltidsstykke-iphone-wallpaper`,
`kalf-still-life-with-9-…` → `after-kalf-still-life-nautilus-iphone-wallpaper`.
Сделать это можно было только сейчас: у вышедшей работы имя файла не меняют,
оно под годовым `immutable`. Старые 44 файла удалены до сборки, проверка
кадров — 6 кадров, худшее расхождение 0.24 при пороге 4.

Карточки разложены по `catalogue/`, 41 ref добавлен в `order.json`,
развеска перемешана (`shuffle-order.mjs`, 410 работ) — партия встала
вразброс, а не блоком. `verify-catalogue.mjs`: 410 записей, 410 адресов.
На витрине видно 158 работ (остальные 252 спрятаны `hidden`), и все 41
новых в карте сайта.

**Расхождение, оставленное как есть.** `ADDING.md` говорит, что слово
`vertical` открывает `alt` всегда. В каталоге это не так: у телефонных
работ 86 из 249, у настольных 18 из 99. Черновики написаны по каталогу,
а не по документу. Что из двух неверно — не решено.

## Чего нет

- Выкладки не было: `./scripts/deploy.sh` не запускался, на боевой машине
  партии нет. Всё описанное — в репозитории и на `127.0.0.1:3000`.
- Десять карточек с `needs` выложены как есть: музей в `credit` назван,
  инвентарный номер у части не подтверждён первоисточником.
- Лист дубликатов (`research/duplicates.html`) и его куски в
  `research/duplicates/` не коммитятся — собираются заново скриптом.
