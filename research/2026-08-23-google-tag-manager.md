# Google Tag Manager на сайте

Дата: 23 августа 2026

## Цель

Поставить контейнер GTM `GTM-NNQTHZS2` на все страницы сайта.

## Принятые решения

**Тег живёт в макете, а не в страницах.** Разметку отдаёт `pages.js`, и все
пять видов документов — указатель, страница работы, приёмка, лицензия,
«не найдено» — проходят через одну функцию `layout()`. Вставка туда закрывает
их разом; вставка в каждую страницу отдельно однажды закончилась бы страницей
без счётчика, и заметили бы это по дырке в отчёте, а не в коде.

**Идентификатор — константа `GTM_ID`, не переменная окружения.** Он всё равно
уезжает в разметке любому посетителю, то есть секретом не является. В `.env`
он бы намекал на тайну и заводил бы способ выложить сайт без счётчика,
о котором никто не узнает.

**Скрипт стоит как можно выше в `<head>`** — сразу за `charset` и `viewport`,
до `<title>`, шрифтов и стилей. Тег, опущенный ниже, теряет визиты тех, кто
ушёл раньше, чем очередь загрузки до него дошла. Загрузка асинхронная
(`j.async=true` в снипете Google), так что отрисовку он не задерживает.

**`<noscript>` с iframe — первым элементом `<body>`,** как требует Google.

Снипет вставлен ровно тем текстом, который выдал GTM: переносы строк внутри
`<script>` сохранены, чтобы его можно было сверить с оригиналом глазами.

## Изменённые файлы

- `pages.js` — константы `GTM_ID`, `gtmHead`, `gtmBody` рядом с `restoreLink`;
  `${gtmHead}` после `<meta name="viewport">`, `${gtmBody}` после `<body>`.

## Проверка

Форматирование:

```
$ npx prettier --check server.js gallery.js limits.js pages.js works.js treatment.js
Checking formatting...
All matched files use Prettier code style!
```

`yarn verify` целиком падает не на коде, а на данных каталога, и падал до этой
задачи: `vl-0371.json` нет в `order.json`, у `vl-0361.json` пуст
`provenance.creator`. К GTM это отношения не имеет.

Живой сервер, число вхождений `GTM-NNQTHZS2` в ответе (ожидается 2 — скрипт
и noscript):

```
== /restore                                          200  2
== /license                                          200  2
== /nope                                             404  2
== /w/the-old-christiansborg-hammershoi-iphone-wallpaper  2
```

Положение в документе на `/`:

```
5:    <meta name="viewport" content="width=device-width,initial-scale=1" />
6:    <script>(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
...
39:  <body>
40:    <noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-NNQTHZS2"
```

## Известные ограничения

- Что именно контейнер собирает, задаётся в интерфейсе GTM, а не здесь. Сайт
  только подключает контейнер; ни одного события в `dataLayer` он пока не
  кладёт.
- Согласия на куки на сайте нет. Пока контейнер пуст, ставить нечего, но
  как только внутрь него попадёт что-то, пишущее куки посетителям из ЕС,
  вопрос встанет — см. LEGAL.md.
- CSP на сайте нет, поэтому загрузке с `googletagmanager.com` ничто не мешает.
  Если CSP появится, домен придётся внести в `script-src` и `frame-src`.
